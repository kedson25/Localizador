rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Emulator-only fixture. This file is deliberately not a production ruleset.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
